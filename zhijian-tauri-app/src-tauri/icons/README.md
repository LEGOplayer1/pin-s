# 图标目录

首次构建前，请准备一张 ≥ 512×512 的 PNG 图标，并命名为 `icon.png` 放入此目录。

然后运行：
```bash
npm run icon
```

这会自动生成所有需要的尺寸（32x32、128x128、256x256、512x512、ico、icns）。

推荐的图标设计风格：
- 简约扁平
- 与莫兰迪色系呼应（米白、雾蓝、淡粉等）
- 可以是便签纸形状或"Z"字母
